# `cargo-xtask-dist-build-readme(1)`

```test
Build the readme files for distribution

Usage: cargo xtask dist-build-readme [OPTIONS]

Options:
  -v, --verbose...  More output per occurrence
  -q, --quiet...    Less output per occurrence
  -h, --help        Print help
```
