[workspace]
members = ["xtask"]

[package]
name = "app"
version = "0.1.0"
edition = "2021"
publish = false
readme = "README.md"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
